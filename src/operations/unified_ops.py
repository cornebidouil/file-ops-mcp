"""
Placeholder module for backwards compatibility.

This module is kept for compatibility with existing code but is not used anymore.
"""
from mcp.server.fastmcp import FastMCP

def register_unified_fileops(mcp: FastMCP) -> None:
    """
    Placeholder function that doesn't register any tools.
    
    Args:
        mcp: The MCP server instance
    """
    # This module is kept for compatibility but doesn't do anything
    pass
